package streamSource;

import java.util.ArrayList;
import java.util.List;

public class SourceOfStream {

    public static void main(String[] args) {
        List<Integer> iList = new ArrayList<>();
        iList.stream().filter().map().collect();
    }
}
